if [[ -n "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.9.2-p290@bbpres" ]] ; then
  \. "$rvm_path/environments/ruby-1.9.2-p290@bbpres"
else
  rvm --create  "ruby-1.9.2-p290@bbpres"
fi
